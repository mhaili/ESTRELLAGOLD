services:
  estrella-gold:
    build: .
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - estrella-network
    volumes:
      - nginx-cache:/var/cache/nginx
      - nginx-logs:/var/log/nginx

  # Service optionnel pour base de données future
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: estrella
  #     POSTGRES_USER: estrella
  #     POSTGRES_PASSWORD: secure_password
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   networks:
  #     - estrella-network

networks:
  estrella-network:
    driver: bridge

volumes:
  nginx-cache:
  nginx-logs:
  # postgres-data: